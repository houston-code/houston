/**
 * Links to Houston's published documents.
 *
 * Houston ships as a plain Apache-2.0 project: there is no separate Terms of Use
 * and no acceptance gate. The LICENSE grants rights to use, modify, and
 * redistribute rather than imposing conditions on running the app, and its
 * warranty disclaimer (section 7) and liability limitation (section 8) apply on
 * their own — there is nothing for an end user to click through. The Privacy
 * document is descriptive, not a contract: it explains where data goes.
 */

/** Base URL for the published documents (repo `main`). */
const DOCS_BASE = 'https://github.com/houston-code/houston/blob/main'

export const LICENSE_URL = `${DOCS_BASE}/LICENSE`
export const PRIVACY_URL = `${DOCS_BASE}/docs/PRIVACY.md`
